region = "ap-south-1"
environment = "portfolio-demo"
instance_type = "t3.small"
root_volume_size = 25
# Set repository_ref to the exact reviewed commit available from the public repository before apply.
# Optional SSH must be a single operator /32; SSM is preferred.
